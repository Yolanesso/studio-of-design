# Инструкция по запуску Telegram бота на сервере

Это руководство поможет вам запустить Telegram бота на сервере с использованием PM2 (менеджер процессов для Node.js).

## 📋 Предварительные требования

1. **Node.js** (версия 18 или выше) - через nvm
2. **npm** или **yarn**
3. **PM2** (будет установлен глобально в активной версии Node.js)
4. **Токен Telegram бота** от @BotFather
5. **Chat ID** (можно узнать у @userinfobot)

## 🚀 Быстрый старт

### 1. Подготовка на сервере

```bash
# Убедитесь, что используете нужную версию Node.js через nvm
nvm use 18  # или другая версия, например: nvm use 20
# Если версия не установлена: nvm install 18

# Проверьте версию Node.js
node -v

# Перейдите в папку с ботом
cd ~/studio-of-design/server

# Установите зависимости
npm install

# Установите PM2 глобально в активной версии Node.js
npm install -g pm2

# Проверьте, что PM2 установлен
pm2 -v
```

### 2. Настройка переменных окружения

```bash
# Создайте файл .env на основе примера
cp env.example .env

# Отредактируйте .env файл
nano .env
```

Заполните следующие переменные:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
PORT=3001
```

### 3. Запуск бота с PM2

```bash
# Запустите бота
npm run pm2:start

# Или напрямую через PM2
pm2 start ecosystem.config.cjs
```

### 4. Проверка статуса

```bash
# Проверьте статус
npm run pm2:status

# Посмотрите логи
npm run pm2:logs
```

## 📝 Полезные команды PM2

### Управление процессом

```bash
# Запуск
npm run pm2:start
# или
pm2 start ecosystem.config.cjs

# Остановка
npm run pm2:stop
# или
pm2 stop telegram-bot-server

# Перезапуск
npm run pm2:restart
# или
pm2 restart telegram-bot-server

# Удаление из PM2
npm run pm2:delete
# или
pm2 delete telegram-bot-server
```

### Мониторинг

```bash
# Просмотр логов
npm run pm2:logs
# или
pm2 logs telegram-bot-server

# Просмотр статуса
npm run pm2:status
# или
pm2 status

# Мониторинг в реальном времени
pm2 monit
```

### Автозапуск при перезагрузке сервера

**Важно при использовании nvm:** PM2 должен использовать правильный путь к Node.js.

```bash
# Сохраните текущую конфигурацию PM2
pm2 save

# Настройте автозапуск при загрузке системы
pm2 startup
# Следуйте инструкциям, которые появятся в терминале
# Обычно нужно выполнить команду вида: sudo env PATH=$PATH:/root/.nvm/versions/node/v18.x.x/bin pm2 startup systemd -u root --hp /root
```

**Альтернативный способ (если pm2 startup не работает с nvm):**

Создайте systemd service файл вручную:

```bash
# Создайте файл сервиса
sudo nano /etc/systemd/system/telegram-bot.service
```

Вставьте следующее содержимое (замените пути на ваши):

```ini
[Unit]
Description=Telegram Bot Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/studio-of-design/server
Environment="PATH=/root/.nvm/versions/node/v18.20.0/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
ExecStart=/root/.nvm/versions/node/v18.20.0/bin/node /root/studio-of-design/server/index.js
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `/root/.nvm/versions/node/v18.20.0` на ваш реальный путь к Node.js. Узнать путь можно командой:
```bash
which node
```

Затем активируйте сервис:
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot.service
sudo systemctl start telegram-bot.service
sudo systemctl status telegram-bot.service
```

## 🔧 Настройка для продакшена

### 1. Создайте папку для логов

```bash
mkdir -p logs
```

### 2. Настройте firewall (если нужно)

```bash
# Откройте порт 3001 (или другой, который вы используете)
sudo ufw allow 3001/tcp
```

### 3. Используйте reverse proxy (рекомендуется)

Для продакшена рекомендуется использовать Nginx как reverse proxy:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 🐳 Альтернатива: Docker (опционально)

Если вы предпочитаете использовать Docker, создайте `Dockerfile`:

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

EXPOSE 3001

CMD ["node", "index.js"]
```

И `docker-compose.yml`:

```yaml
version: '3.8'

services:
  bot:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: unless-stopped
```

Запуск:
```bash
docker-compose up -d
```

## 🔍 Отладка

### Проверка работы бота

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Проверьте логи: `npm run pm2:logs`

### Проверка API

```bash
# Health check
curl http://localhost:3001/api/health

# Тест отправки формы (замените данные на свои)
curl -X POST http://localhost:3001/api/submit-form \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест",
    "location": "Москва",
    "phone": "+7 999 123 45 67",
    "email": "test@example.com",
    "message": "Тестовое сообщение",
    "consent": true
  }'
```

## ⚠️ Важные замечания

1. **Безопасность**: Никогда не коммитьте файл `.env` в Git
2. **Токен бота**: Храните токен в безопасности
3. **Chat ID**: Убедитесь, что вы написали боту `/start` перед использованием
4. **Порты**: Убедитесь, что порт 3001 (или другой) не занят другим приложением
5. **nvm и PM2**: При использовании nvm убедитесь, что PM2 установлен в активной версии Node.js. Если после перезагрузки PM2 не работает, проверьте, что nvm инициализирован в вашем shell (обычно через `~/.bashrc` или `~/.zshrc`)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `npm run pm2:logs`
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что бот запущен: `npm run pm2:status`
4. Убедитесь, что вы написали боту `/start` в Telegram

