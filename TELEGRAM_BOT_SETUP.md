# Настройка Telegram бота для уведомлений о заявках

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например: "Studio Design Notifications")
4. Введите username для бота (должен заканчиваться на `bot`, например: `studio_design_bot`)
5. Скопируйте полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Узнать свой Chat ID

1. Откройте Telegram и найдите бота **@userinfobot**
2. Отправьте команду `/start`
3. Бот вернет ваш Chat ID (число, например: `123456789`)

## Шаг 3: Настройка сервера

1. Перейдите в папку `server`:
```bash
cd server
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в папке `server`:
```bash
# Windows
copy env.example .env

# Linux/Mac
cp env.example .env
```

4. Откройте файл `server/.env` и заполните его:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
PORT=3001
```

## Шаг 4: Запуск сервера

В папке `server` выполните:
```bash
# Обычный режим
npm start

# Режим разработки (с автоперезагрузкой)
npm run dev
```

Сервер будет доступен на `http://localhost:3001`

## Шаг 5: Настройка фронтенда (опционально)

Если ваш backend сервер работает на другом URL (не localhost:3001), создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://your-server-url:3001
```

## Проверка работы

1. Убедитесь, что сервер запущен
2. Откройте сайт и заполните форму заявки
3. Отправьте форму
4. Проверьте Telegram - должно прийти уведомление с данными заявки

## Формат уведомления

Бот будет отправлять сообщения в следующем формате:

```
🔔 Новая заявка с сайта

📅 Дата: 01.01.2024, 12:00:00

👤 Имя: Иван Иванов
📍 Локация: Москва
📞 Телефон: +7 999 123 45 67
📧 Email: ivan@example.com

💬 Сообщение:
Хочу заказать дизайн интерьера

✅ Согласие на обработку данных: Да
```

## Устранение проблем

### Бот не отправляет сообщения

1. Проверьте, что токен бота правильный
2. Проверьте, что Chat ID правильный
3. Убедитесь, что вы написали боту хотя бы одно сообщение (чтобы бот мог вам писать)

### Ошибка подключения

1. Проверьте, что сервер запущен
2. Проверьте, что порт 3001 не занят другим приложением
3. Проверьте настройки CORS (если фронтенд на другом домене)

### Ошибка "Unauthorized"

- Проверьте правильность токена бота
- Убедитесь, что токен не содержит лишних пробелов

